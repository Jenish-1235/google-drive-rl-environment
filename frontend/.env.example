# API Base URL
# For development: http://localhost:5000/api
# For production (Docker): /api (uses nginx proxy)
# Override this if your backend is running on a different port or host
VITE_API_BASE_URL=

# Dummy user credentials for auto-login (used in Docker deployment)
VITE_DUMMY_USER_EMAIL=demo@drive.com
VITE_DUMMY_USER_PASSWORD=demo123
